# management.yml

management:
  endpoints:
    web:
      exposure:
        include: health, info
#        include: "*"
  endpoint:
    health:
          show-details: always  # Show detailed health information
  mongo:
    enabled: true
